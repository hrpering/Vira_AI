version: 0.1
name: balanced-turkish-writing
task:
  language: en
  class: structured-writing
constraints:
  max_latency_ms: 8000
  require_citations: false
candidates:
  - id: account-available-model
    reason: Verified by the live account catalog and suitable for English structured writing.
fallbacks:
  - id: account-available-model
    when: timeout-or-capacity
disclosure:
  show_selected_model: true
  show_fallback: true
