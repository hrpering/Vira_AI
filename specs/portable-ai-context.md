# Portable AI Context spec v0.1

Taşınabilir AI context; `context_id`, `version`, `source`, `messages`, `artifacts`, `decisions`, `permissions` ve `retention` alanlarını kapsar. İstemci, bilmediği uzantıları koruyabilir ancak güvenilmeyen talimatları otomatik çalıştırmamalıdır.
